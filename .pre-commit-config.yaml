repos:
  - repo: local
    hooks:
      - id: lint
        name: npm run lint
        entry: npm run lint
        language: system
        pass_filenames: false
        stages: [pre-commit]

      - id: build
        name: npm run build
        entry: npm run build
        language: system
        pass_filenames: false
        stages: [pre-commit]
